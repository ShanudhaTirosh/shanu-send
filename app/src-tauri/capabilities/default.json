{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities available to the main ShanuSend window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
